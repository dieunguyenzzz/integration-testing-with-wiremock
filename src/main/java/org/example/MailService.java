package org.example;

public class MailService {
    public boolean sendEmail(String to, String subject, String body) {
        // Code to send email goes here
        // For simplicity, let's assume the email is always successfully sent
        return true;
    }
}
